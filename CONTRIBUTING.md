# 参与贡献

感谢你愿意改进墨读。

## 提交问题

提交 Issue 前请先搜索是否已有相同问题，并尽量提供：

- 浏览器和操作系统版本
- 可复现问题的最小 Markdown 示例
- 操作步骤、预期行为和实际行为
- 必要时提供截图，但请先移除敏感内容

请不要在公开 Issue 中上传机密文档。

## 本地开发

墨读是静态项目，不需要安装依赖：

```bash
npm run serve
npm test
```

访问 `http://127.0.0.1:4173/`，修改源码后刷新页面即可。

## Pull Request

1. 从 `main` 创建主题分支。
2. 保持修改范围清晰，避免混入无关格式调整。
3. 新增纯逻辑时，优先放入 `assets/core.js` 并添加测试。
4. 运行 `npm run check`。
5. 在 PR 中说明动机、实现方式、验证结果和界面变化。

## 代码约定

- 使用两个空格缩进、UTF-8 和 LF 换行。
- 不引入需要联网加载的运行时资源。
- 新依赖必须有明确价值，固定版本，并更新第三方软件声明。
- 保持键盘操作、焦点状态和移动端布局可用。
- 不收集文档内容、文件名或使用行为。
