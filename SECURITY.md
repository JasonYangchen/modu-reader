# 安全策略

## 支持范围

安全修复优先应用于最新的 `main` 分支和最新发布版本。

## 报告安全问题

请通过 GitHub 仓库的 **Security → Report a vulnerability** 私下报告，不要公开创建 Issue。报告中请包含影响、复现步骤、受影响浏览器以及建议修复方式。

## 安全边界

- Markdown 文件在浏览器本地读取，不会由墨读上传。
- 渲染结果会移除脚本、表单、嵌入对象、事件处理属性与危险 URL。
- 外部链接使用 `noopener` 和 `noreferrer`。
- 项目不提供加密存储、文件权限隔离或恶意软件检测。

安全过滤属于纵深防御，不能代替浏览器安全更新。请避免打开来源不明或包含敏感信息的文件。
